import styled from 'styled-components';

export const Container = styled.div`
    margin: 16px auto;
    width: 90%;
    max-width: 1140px;

    h1 {
        font-weight: 300;
        text-transform: uppercase;
        display: flex;
        align-items: center;
        font-size: 30px;
        color: white;
        svg {
            margin-right: 8px;
        }
    }
`;
